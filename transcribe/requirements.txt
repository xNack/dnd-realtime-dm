# =============================================================================
#  requirements.txt — elenco delle librerie Python per la TRASCRIZIONE (Whisper).
#
#  A cosa serve: lo legge il comando  pip install -r requirements.txt  per
#  installare in automatico tutti i pacchetti necessari, nella versione giusta.
#  Riguarda SOLO lo step opzionale di trascrizione (gira sul tuo computer, non
#  dentro Docker); la pipeline vera e propria non ne ha bisogno.
#
#  Nota: usa Python 3.12 (Whisper/PyTorch non supportano ancora 3.14).
#  Le versioni sono "pinnate" (fissate) per evitare conflitti su Mac Intel:
#   - numba/llvmlite 0.60: hanno il pacchetto pronto (niente compilazione con CMake)
#   - numpy < 2: richiesto da PyTorch 2.2 (con numpy 2.x Whisper va in errore)
# =============================================================================
openai-whisper>=20231117
yt-dlp>=2024.0.0
numba==0.60.0
numpy<2

# Serve anche ffmpeg installato a livello di sistema (vedi GUIDA.md).
